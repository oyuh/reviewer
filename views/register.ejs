<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Review - Register</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        *::-webkit-scrollbar-track
        {
            background-color: #252525;
        }

        *::-webkit-scrollbar
        {
            width: 12px;
            background-color: #100c20;
        }

        *::-webkit-scrollbar-thumb
        {
            background-color: #100c20;
        }
    </style>
</head>
<body class="bg-gray-900 text-white flex flex-col items-center justify-center min-h-screen">
    <header class="bg-gray-800 w-full py-4 fixed top-0 left-0 flex justify-between items-center px-8">
        <div class="flex items-center">
            <a href="/" class="text-white mr-4">
                <i class="fas fa-home"></i>
            </a>
        </div>
        <div class="flex items-center">
            <% if (user) { %>
                <span class="text-purple-400 mr-4">Hello, <%= user.username %>!</span>
                <a href="/logout" class="text-white mr-4">Logout</a>
                <a href="/profile" class="text-white mr-4">Profile</a>
                <% if (user.isAdmin) { %>
                    <a href="/admin" class="text-white mr-4">Admin</a>
                <% } %>
            <% } else { %>
                <a href="/login" class="text-white mr-4">Login</a>
                <a href="/register" class="text-white">Register</a>
            <% } %>
        </div>
    </header>
    <div class="flex flex-col items-center justify-center w-full min-h-screen pt-24 pb-24">
        <div class="bg-gray-800 p-10 rounded-lg shadow-lg w-96">
            <h1 class="text-3xl font-bold text-purple-400 mb-6 text-center">Register</h1>
            <% if (errorMessage) { %>
                <div class="text-red-500 mb-4"><%= errorMessage %></div>
            <% } %>
            <form action="/register" method="POST" class="flex flex-col">
                <input type="text" name="username" placeholder="Username" required class="bg-gray-700 text-white p-3 rounded mb-4 focus:outline-none focus:ring-2 focus:ring-purple-400">
                <input type="password" name="password" placeholder="Password" required class="bg-gray-700 text-white p-3 rounded mb-6 focus:outline-none focus:ring-2 focus:ring-purple-400">
                <button type="submit" class="bg-purple-500 text-white py-2 rounded hover:bg-purple-600 transition duration-200">Register</button>
            </form>
        </div>
    </div>
    <footer class="bg-gray-800 w-full py-4 fixed bottom-0 left-0 text-center">
        <p>Lawson Hart | &copy; 2024 </p>
    </footer>
</body>
</html>
